# 试运行期 · 第二阶段非敏感运营日志

本日志记录真实 PR 的**聚合、去标识化**运营数据，用于 P2 复盘。它不是扫描报告：不得填入源码、路径、行号、evidence、原始扫描 JSON、PR diff、密钥、凭据或 DeepSeek 请求/响应。终态人工判定的权威记录仍在 `.codesec/triage.json`；这里仅保留可复盘的汇总和索引。

使用规则：每个真实 PR 在扫描完成后登记一行；人工处置完成后补齐时间与计数；每周汇总一次。真实 PR 的定义、指标算法和退出阈值见[第二阶段运营规范](trial-phase-2.md)。

## 0. 启动快照（不计入 P2 样本）

| 日期 | 基线说明 | finding 数 | 已有 disposition | 说明 |
| --- | --- | ---: | ---: | --- |
| 2026-08-24 | 已合并的初始 baseline | 17 | 1（`bandit:B404` → `false_positive`） | 历史本地基线，仅作上下文；不计入真实 PR、confirmed 覆盖或 P2 退出条件。 |

## 1. PR 运行记录

| 周次 / 日期 | PR / run 链接 | 真实 PR？ | 新增 / 变更相关 / error finding | 规则 ID 汇总 | finding 总数 | 人工复核数 | 后端汇总（gated / deterministic / 无候选） | 扫描完成时间（UTC） | 最后终态处置时间（UTC） | 已处置耗时（小时） | 非敏感备注 |
| --- | --- | --- | ---: | --- | ---: | ---: | --- | --- | --- | ---: | --- |
|  |  | 是 / 否 |  |  |  |  |  |  |  |  |  |

只记录公开 PR/run 链接；若链接本身或标题会暴露敏感信息，以内部受控索引替代，不能复制报告内容。

## 2. 每周聚合指标

| 周次 | 真实 PR 数 | finding 总数 | 每 PR finding 数 | 人工复核数 | 每 PR 人工复核数 | 终态 disposition 数（累计） | 可比较处置数 | 一致率 | FP 率（按 tool/rule 另见 §3） | 处置时长中位数 / 均值（小时） | gated / deterministic / 无候选 PR | 两轴结论 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | --- | --- | --- | --- |
| 第 1 周 |  |  |  |  |  |  |  |  |  |  |  |  |

“两轴结论”必须分别写准确率与运营负担是否达到[运营规范 §3](trial-phase-2.md#3-校准样本与两条度量轴)的初始目标；不能用单一“正常”代替。

## 3. 规则级校准覆盖

| tool / rule | 机器标签 | 终态人工判定计数（TP / needs fix / FP / accepted risk） | 一句话、非敏感依据 | 处置数 | FP 率 | confirmed 覆盖计入？ | 可调整判断？（≥3） | P2 结论（保留 / 调整 / 排除） |
| --- | --- | --- | --- | ---: | ---: | --- | --- | --- |
|  |  |  |  |  |  | 是 / 否 | 是 / 否 |  |

将 `suspicious` 单列记录，不计入机器/人工一致率的分母。`accepted_risk` 不是 FP；它表示 finding 有效但被业务接受。`exec-detected`、`use-defused-xml` 等 confirmed 类规则未自然出现时，填写“保留（样本不足，不调整）”并在备注中标为覆盖缺口。

## 4. 去标识化漏报种子（仅供未来 P4 讨论）

| 发现周 | PR / run 或受控索引 | 风险类别 / 漏洞家族 | 风险依据（非敏感） | 未命中原因 | 候选 detector / rule 方向 | 影响范围（粗粒度） | 处置状态 |
| --- | --- | --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |  | 仅记录 / 已在 P2 复盘确认 |

本表不得包含源码、path、行号、evidence 或原始扫描产物；达到同一漏洞家族或语言 3 条后，才可满足“扫描规则扩展”的重新讨论条件；**本期不实现任何新规则**。

## 5. P2 复盘决策表

当[运营规范 §4](trial-phase-2.md#4-p2-复盘退出标准与结论格式)的退出条件全部满足时，逐项填写。每项只能选“保留、调整或排除”；样本不足选“保留（样本不足，不调整）”，不能写“待观察”。

| tool / rule / 运营点 | 样本与人工判定 | 准确率数据 | 运营负担数据 | 结论（保留 / 调整 / 排除） | 数据依据与 owner | 后续 PR / 日期 |
| --- | --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |  |
